# gstack Codex Ship Prompt

Run `gstack-ship` on the current branch.

Expectations:
- Sync with the base branch before final verification.
- Run the relevant test suite and stop on blocking failures.
- Update docs if behavior or setup changed.
- Push the branch and open the PR only after the branch is ready to review.
